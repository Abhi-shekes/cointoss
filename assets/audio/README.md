# Audio assets (Phase 4)

Drop these files here; `SoundEngine` already references them and no-ops until they exist:

- `ambient_bar.mp3` — looping smoky-bar ambience (low jazz / gramophone)
- `whoosh.mp3` — coin launch / spin whoosh
- `clink.mp3` — metallic landing on the table
- `reveal.mp3` — result sting

Keep them short, mono, ~128 kbps to stay light in the APK.
